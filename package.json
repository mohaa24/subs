{
  "name": "subs",
  "private": true,
  "workspaces": [
    "fe",
    "be"
  ],
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"cd be && npm run dev\" \"cd fe && npm run dev\"",
    "dev:be": "cd be && npm run dev",
    "dev:fe": "cd fe && npm run dev",
    "build": "npm run build -w fe",
    "seed": "npx -w be prisma db seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
